// Type Inference
let message2 = "mensagem definida" // definir o tipo da variável implicitamente, apenas passando o seu valor.
message2 = "string nova"

window.addEventListener("click", (e) => {
    console.log(e.target)
})